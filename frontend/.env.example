# Leave empty when frontend and Laravel API are served from the same domain.
# For local Vite dev, .env.development points to http://127.0.0.1:8000.
# Set this during production build only if the frontend is hosted separately from the Laravel API.
VITE_API_BASE_URL=
